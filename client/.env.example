# Client Environment Configuration
# Copy this file to .env and update with your values

# Application Branding
VITE_APP_NAME=System Dashboard
VITE_APP_DESCRIPTION=Serverless web application template

# API URL - Update this after deploying your Lambda function
VITE_API_URL=http://localhost:3001

# Stripe Public Key (if using payments)
# VITE_STRIPE_PUBLIC_KEY=pk_test_your_stripe_public_key

# Pay Service Configuration (for pay-auth-integration)
# Uses backend proxy to external pay service (recommended)
VITE_PAY_URL=http://localhost:3001

# Alternative: Direct external access (requires CORS configuration)
# VITE_PAY_URL=https://pay.jeffreykeyser.net

# Add any other client-side environment variables here
# Note: All variables must be prefixed with VITE_ to be available in the client

# NPM Token for private package authentication (if needed locally)
# NPM_TOKEN=your-github-token-here