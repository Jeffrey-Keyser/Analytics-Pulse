# setup-config.env.example
# Copy to setup-config.env and customize for non-interactive setup
# The setup scripts will source this file if it exists
#
# Usage:
#   cp setup-config.env.example setup-config.env
#   # Edit setup-config.env with your values
#   ./setup-project.sh --non-interactive
#
# Note: DO NOT commit setup-config.env (it's in .gitignore)

# ============================================
# Core Configuration
# ============================================

# Service name (lowercase, no spaces)
SETUP_SERVICE_NAME=my-new-service

# Domain name for your application
SETUP_DOMAIN_NAME=example.com

# GitHub repository name
SETUP_GITHUB_REPO=my-new-service-repo

# GitHub owner/organization
SETUP_GITHUB_OWNER=Jeffrey-Keyser

# AWS region
SETUP_AWS_REGION=us-east-1

# Environment (dev/staging/prod)
SETUP_ENVIRONMENT=prod

# Shared infrastructure state bucket
SETUP_SHARED_INFRA_BUCKET=shared-infra-terraform-state

# Database schema name
SETUP_DATABASE_SCHEMA=public

# ============================================
# CORS Configuration
# ============================================

# CORS allowed origins (comma-separated for multiple)
# Examples:
#   Single origin: https://example.com
#   Multiple origins: https://example.com,https://www.example.com
SETUP_CORS_ORIGINS=https://example.com

# Allow credentials (cookies, JWT tokens) in cross-origin requests
# Values: true or false
SETUP_CORS_CREDENTIALS=true

# CORS allowed HTTP methods (comma-separated)
SETUP_CORS_METHODS=GET,POST,PUT,DELETE,OPTIONS

# CORS allowed headers (comma-separated)
SETUP_CORS_HEADERS=Content-Type,Authorization,X-Requested-With

# ============================================
# Authentication Configuration
# ============================================

# Use GitHub OIDC for AWS authentication (recommended)
# Values: true or false
# - true: Uses GitHub OIDC (no AWS credentials needed)
# - false: Uses static AWS credentials (requires AWS access keys)
SETUP_ENABLE_OIDC=true

# ============================================
# GitHub Configuration
# ============================================

# GitHub Personal Access Token
# Required for:
#   - Terraform provider authentication
#   - Accessing private GitHub Actions
#   - Installing private npm packages
#
# You can also set this via:
#   export TF_VAR_github_token="your_token"
#   export GITHUB_TOKEN="your_token"
#
# Note: If set via environment variable, this takes precedence
# TF_VAR_github_token=your-github-token-here
# GITHUB_TOKEN=your-github-token-here

# ============================================
# Optional: Git Repository URL
# ============================================

# Git repository URL (optional)
# Only used if git remote 'origin' is not already configured
# SETUP_REPO_URL=git@github.com:username/repo.git

# ============================================
# Usage Examples
# ============================================

# Example 1: Basic non-interactive setup
#   cp setup-config.env.example setup-config.env
#   # Edit setup-config.env with your values
#   export GITHUB_TOKEN="your_token"
#   ./setup-project.sh --non-interactive

# Example 2: Using environment variables only
#   export SETUP_SERVICE_NAME="my-service"
#   export SETUP_DOMAIN_NAME="example.com"
#   export GITHUB_TOKEN="your_token"
#   ./setup-project.sh --non-interactive

# Example 3: Mix of config file and environment variables
#   # Config file provides defaults
#   # Environment variables override config file
#   cp setup-config.env.example setup-config.env
#   export SETUP_SERVICE_NAME="override-service-name"
#   export GITHUB_TOKEN="your_token"
#   ./setup-project.sh --non-interactive

# ============================================
# Precedence Order
# ============================================
#
# When running in non-interactive mode:
# 1. Environment variables (highest priority)
# 2. Config file values (setup-config.env)
# 3. Default values (lowest priority)
#
# When running in interactive mode:
# 1. Environment variables (if set, skips prompt)
# 2. Config file values (if set, skips prompt)
# 3. User input from prompts
# 4. Default values
